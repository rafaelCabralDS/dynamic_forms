name: dynamic_forms
description: A new Flutter package project.
version: 0.19.0
homepage: https://github:com/rafaelCabralDS/dynamic_forms

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
    
  mask_text_input_formatter: ^2.5.0
  file_picker: ^5.5.0
  syncfusion_flutter_datagrid: ^23.1.36
  syncfusion_flutter_core: ^23.1.36
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

